{
  "name": "data-provenance-verifier",
  "version": "1.0.0",
  "private": true,
  "description": "Verify that external data files in a repository are genuine, not fabricated by an AI agent.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "type": "module"
}
